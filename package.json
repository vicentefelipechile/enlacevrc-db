{
	"name": "enlacevrc-db",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"db-create": "wrangler d1 execute enlacevrc-db --file=./db/schema.sql",
		"db-test": "wrangler d1 execute enlacevrc-db --file=./db/test.sql",
		"generate-pk": "node src/generate-key.js"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.40.2"
	}
}